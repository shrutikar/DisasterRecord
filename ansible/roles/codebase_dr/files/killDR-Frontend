#!/bin/bash
TS=$(date -u)
echo -e "[$TS] -->KILLING DRECORD FRONTEND SERVICE\n" >> /var/log/DR.log 2>&1 &
pid=$(cat /root/drService_Frontend.pid)
while true; do
  cpid=$(pgrep -P $pid)
  if [[ $cpid ]]; then
    echo $cpid
    kill $cpid
    pid=$cpid
  else
    break
  fi
done
echo $pid
kill $pid